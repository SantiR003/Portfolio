import React from 'react'
import ReactDOM from 'react-dom/client'
import { PagePortfolio } from './Components/Page'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PagePortfolio />
  </React.StrictMode>,
)
